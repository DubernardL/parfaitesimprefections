<!-- NAV BAR -->
<nav class="navbar navbar-expand-lg navbar-light bg-light" id="navbar-id">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>"><i class="fas fa-home"></i><span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Catégories
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <% @categories.each do |category| %>
            <a class="dropdown-item" href="<%= category_path(category) %>"><%= category.name %></a>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
  <a class="navbar-brand" href="<%= root_path %>">Parfaites Imperfections</a>
  <div class="collapse navbar-collapse" id="navbarNavDropdown" id="right-title">
    <ul class="navbar-nav">


      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          A propos
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <a class="dropdown-item" href="#">L'atelier</a>
          <a class="dropdown-item" href="<%= new_contact_path %>">Contact</a>
        </div>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= button_to "Déconnexion", destroy_user_session_path, method: :delete, class: "btn btn-secondary" %>
        <% else %>
          <%= button_to "Connexion", new_user_session_path, class: "btn btn-secondary" %>
        <% end %>
      </li>
      <li class="nav-item active">
        <% if user_signed_in? %>
        <a class="nav-link" href="<%= cart_path(@cart) %>"><i class="fas fa-shopping-cart"></i></a>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
